<!-- include static header -->
<%- include ('partials/header.ejs') %>


<!-- main content -->
<div class="container">
    <br>
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="col">
                    <br>
                    <div class="row">
                        <div class="col-9">
                            <h1><%= data.name %></h1>
                            <p><%= data.short_description %></p>
                        </div>
                        <div class="col-3">
                            <p style="text-align: right">
                                <i class="far fa-clock"></i> <%= data.duration %>min<br>
                                <i class="far fa-user"></i> <a href="/user/<%= data.creator_id %>"><%= data.creator %></a>
                                <% if(session && session.privileges == 'A') { %>
                                <br>
                                <a href="/dish/<%= data.id %>/assign"><span class="badge badge-danger float-right">Rezept übernehmen</span></a>
                                <% } %>
                            </p>
                        </div>
                    </div>

                    <div class="dropdown-divider"></div>

                    <img src="/uploads/<%= data.image_name %>" class="img-thumbnail" id="dishImage">

                    <div class="dropdown-divider"></div>

                    <p><h2>Zutaten</h2></p>

                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th>Einheit</th>
                            <th>Zutat</th>
                        </tr>
                        </thead>
                        <tbody>
                            <% for(let i=0;i<data.ingredients.length;i++) { %>
                                <tr>
                                    <td><%= data.ingredients[i].count %></td>
                                    <td><p data-toggle="tooltip" title="<%= data.ingredients[i].unit_name %>"><%= data.ingredients[i].short_name %></p></td>
                                    <td><%= data.ingredients[i].name %></td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>

                    <div class="dropdown-divider"></div>

                    <p><h2>Zubereitung</h2></p>
                    <p style="white-space: pre-line"><%- data.preparation %></p>
                    <br>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- include footer -->
<%- include ('partials/bottom.ejs') %>
<%- include ('partials/footer.ejs') %>